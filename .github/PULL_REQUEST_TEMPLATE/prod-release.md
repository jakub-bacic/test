# Changelog

### Features
- GF-XXX: feature name

### Bugfixes
- GF-XXX: bug fix name

### Others
- GF-xxx: chore/refactor/CI etc.

# TODO LIST
**WARNING:** Do NOT update the branch even if Github suggests it.

- [ ] Make sure PR targets `main` branch.
- [ ] Update PR title to include current date (add `(1)`, `(2)` etc. in case of conflicts).
- [ ] Update changelog template above based on commit messages.
- [ ] Ask someone to stamp your PR. Make sure all changes are safe to be deployed to production.
- [ ] Click merge and ensure the commit message is equal to PR title (e.g. `Release 2025-08-20`).

